#include <bits/stdc++.h>
using namespace std;
class BankCustomer {
    private:
	string name;
	string address;
	string mobile;
	string birth_of_date;
	public:
	    get_data()// return name,address,mobile and birth_of_date
	    set_data()// enter customer data
};

int main() {
  BankCustomer x;
  x.set_data();
  x.get_data();

	return 0;
}
